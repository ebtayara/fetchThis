List edit:
-[x] add more task to a list
    as we add a task it should be added to a list specified

Task edit:
-[x] Editing name
-[x] add description to tasks


Working with PostMan
-[x] remove requireAuth
-[x] insert UserId because we don't need to use the session object
-[x] res.send
        res.send(JSON.stringify(lists));
